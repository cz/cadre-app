import { StyleSheet } from 'react-native';
import { colors } from '../../config/styles';

const styles = {
  rowContents: {
    flexDirection: 'row',
    width: 64,
    height: 64,
  },
};

export default styles;
